<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baizhi.ems.dao.EmpDAO">

    <select id="findAll" resultType="Emp">
        select rownum,t.id,t.name,t.salary,t.age from
        (select row_number() over() as rownum,* from t_emp) t where rownum &lt;= 10
    </select>

    <insert id="save" parameterType="Emp">
        <selectKey resultType="int" keyProperty="id" order="BEFORE">
            select nextval('t_emp_seq') as id
        </selectKey>
        insert into t_emp  values (#{id},#{name},#{salary},#{age})
    </insert>

    <delete id="delete" parameterType="String">
        delete from t_emp where id = #{id}
    </delete>

    <select id="findById" parameterType="String" resultType="Emp">
        select id,name,salary,age from t_emp where id = #{id}
    </select>

    <update id="update" parameterType="Emp">
        update t_emp set name=#{name},salary=#{salary},age=#{age} where id = #{id}
    </update>


</mapper>